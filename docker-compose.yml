version: '3.8'

services:
  app:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./uploads:/app/uploads
    depends_on:
      - postgres
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=password
      - DB_NAME=vwings24x7_db

  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: vwings24x7_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8004:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
